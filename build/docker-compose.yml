version: "3.8"
services:
  db:
    image: postgres
    ports:
    - 5432:5432
    volumes:
    - "db_data:/var/lib/postgresql/data"
    environment:
      POSTGRES_PASSWORD: password
volumes:
  db_data: